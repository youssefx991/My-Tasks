@page "/tracks/details/{id:int}"

<h3>Details</h3>

@code {
    [Parameter]
    public int id { get; set; }

    public Track? track { get; set; }

    protected override Task OnInitializedAsync()
    {
        var track = TracksTreineesContext.GetTrackByID(id);
        if (track != null)
        {
            this.track = track;
        }

        return base.OnInitializedAsync();
    }
}

@if (track == null)
{
    <p>Loading Track Details.........</p>
}
else
{

    @if (track == null)
    {
        <div class="text-center">
            <p class="text-muted">Loading Trainee Details...</p>
        </div>
    }
    else
    {
        <div class="row mb-2">
            <div class="col-sm-3 fw-bold">Name:</div>
            <div class="col-sm-9">@track.Name</div>
        </div>

        <div class="row mb-2">
            <div class="col-sm-3 fw-bold">Description:</div>
            <div class="col-sm-9">@track.Description</div>
        </div>
        

        <div class="row mb-2">
            <div class="col-sm-3 fw-bold">Track Trainees</div>
            @foreach (var trainee in TracksTreineesContext.GetTraineesByTrackID(track.ID))
            {
                <div class="col-sm-9">@trainee.Name</div>
            }
        </div>
    }

           
    <a href="/tracks" class="btn btn-secondary btn-sm">Back to List</a>











}
